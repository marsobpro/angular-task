import { UploadAgeDirective } from './upload-age.directive';

describe('UploadAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadAgeDirective();
    expect(directive).toBeTruthy();
  });
});
